<van-cell-group>
  <van-field value="{{ username }}" clearable label="用户名" placeholder="请输入用户名" />
  <van-field value="{{ message }}" label="个人简介" type="textarea" placeholder="请输入简介" autosize border="{{ false }}" />
</van-cell-group>

<view class="btn">
  <van-button type="info" bindtap="confirm">确认修改</van-button>
</view>